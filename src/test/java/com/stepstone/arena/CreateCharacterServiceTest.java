package com.stepstone.arena;

//import static com.stepstone.arena.CreateCharacterService.drawRandomNumber;

import org.hamcrest.Matcher;
import org.hamcrest.Matchers;
import org.junit.Assert;
import org.junit.Test;
import org.junit.jupiter.api.Assertions;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.Random;

//@RunWith(SpringRunner.class)
//@SpringBootTest
public class CreateCharacterServiceTest {

  private static final int minNumber = 5;
  private static final int maxNumber = 15;

//  @Test
//  public void checkIfDrawRandomNumberReturnIntFromScope() {
//    Assert.assertTrue(minNumber < drawRandomNumber(5, 15) && drawRandomNumber(5, 15) < maxNumber);
//  }
//
//  @Test
//  public void checkIfDrawRandomNumberFailWithInproperValues() {
//    Assert.assertFalse(minNumber < drawRandomNumber(40, 50) && drawRandomNumber(40, 50) < maxNumber);
//  }
//
//  @Test
//  public void checkThatDrawRandomNumberContainEdgeNumber() {
//    Assert.assertTrue(minNumber < drawRandomNumber(minNumber, maxNumber) && drawRandomNumber(minNumber, maxNumber) < maxNumber);
//  }

}
